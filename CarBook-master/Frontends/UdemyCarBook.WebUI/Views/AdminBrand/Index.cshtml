@model List<ListBrandDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-flex align-items-center justify-content-between">
            <h4 class="mb-0 font-size-18">Brands</h4>

            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a asp-controller="AdminDashboard" asp-action="Index">Dashboard</a></li>
                    <li class="breadcrumb-item active">Brand</li>
                </ol>
            </div>

        </div>
    </div>
</div>
<!-- end page title -->

<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Brands Table</h4>
                <p class="card-subtitle mb-4">
                    You can do CRUD operations on that table.
                </p>
                <div class="table-responsive">
                    @if (Model.Count < 1)
                    {
                        <p><b>There is no Brand yet.Please start creating one.</b></p>
                    }
                    else
                    {
                        <table class="table mb-0">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Brand</th>
                                    <th>Delete</th>
                                    <th>Update</th>
                                    <th>Cars</th>


                                </tr>
                            </thead>
                            <tbody>
                                <script type="text/javascript">
                                    function confirmDelete() {
                                        return confirm("Are you sure you want to delete this feature?");
                                    }
                                </script>

                                @foreach (var item in Model)
                                {
                                    count++;
                                    <tr>
                                        <th scope="row">@count</th>
                                        <td>@item.Name</td>
                                        <td><a asp-controller="AdminBrand" asp-action="RemoveBrand" asp-route-id="@item.BrandID" class="btn btn-outline-danger" onclick="return confirmDelete();">Delete</a></td>
                                        <td><a asp-controller="AdminBrand" asp-action="UpdateBrand" asp-route-id="@item.BrandID" class="btn btn-outline-success">Update</a></td>
                                        <td><a asp-controller="AdminBrand" asp-action="UpdateBrand" asp-route-id="@item.BrandID" class="btn btn-outline-dark">Cars</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    <br />
                    <a asp-controller="AdminBrand" asp-action="CreateBrand" class="btn btn-outline-primary">Add New Brand</a>
                </div>

            </div>
            <!-- end card-body-->
        </div>
        <!-- end card -->

    </div>
    <!-- end col -->



</div>


